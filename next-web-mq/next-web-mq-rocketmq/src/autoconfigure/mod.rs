//! Auto-configuration components for the RocketMQ starter.

/// Defines the default bootstrap sequence that wires properties, listeners, and
/// the template service into the application context.
pub mod rocketmq_auto_configuration;

/// Declares the external configuration properties consumed by the starter.
pub mod rocketmq_properties;
